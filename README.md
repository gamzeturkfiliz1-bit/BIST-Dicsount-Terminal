BIST-Discount-Terminal 
Project Technical Note:
This application fulfills the database requirement by utilizing the browser's Web Storage API (LocalStorage) as a persistent client-side database. It securely stores user session data and ticker preferences, ensuring data persistence across sessions without requiring external server dependencies.
